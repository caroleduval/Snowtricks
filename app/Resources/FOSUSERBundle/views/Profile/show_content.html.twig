{% trans_default_domain 'FOSUserBundle' %}

    <div class="container">
        <div class="col-xs-12 col-sm-6 col-sm-offset-3 col-lg-4 col-lg-offset-4">
            <div class="well">
                <legend>Mon profil</legend>
                <div class="form-group">
                    <label>Prénom :</label>
                    <div class="champ">{{ user.username|capitalize }}</div>
                </div>
                <div class="form-group">
                    <label>Nom :</label>
                    <div class="champ">{{ user.name|capitalize }}</div>
                </div>
                <div class="form-group">
                    <label>Email :</label>
                    <div class="champ">{{ user.email }}</div>
                </div>
                <div class="form-group">
                    <label>Photo :</label>
                    {% if user.photo is not null %}
                        <div class="champbis"><img src="{{ asset(user.photo.webPath) }}" alt="{{ user.name }}" width="60px"></div>
                    {% else %}
                        <div class="champ">Pas de photo de profil</div>
                    {% endif %}
                </div>
                <div class="form-group text-center">
                    <a class="btn btn-primary btn-lg" href="{{ path('fos_user_profile_edit') }}">Modifier</a>
                </div>
            </div>
        </div>
    </div>
